#
# tmux.conf

### prefixをC+zに変更
set -g prefix C-z

### デフォルトのprefixキーを解除
unbind C-b

###
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",$TERM:Tc" # tmuxを起動していない時のzshでの

### zsh
{% if yadm.class == "zsh" %}
if-shell "test -f /usr/bin/zsh" "set-option -g default-shell /usr/bin/zsh"
{% else %}
# there is no zsh in local.class
{% endif %}

### 
bind-key C-z last-window
###
bind-key -n C-t send-prefix

### スクロールバッファサイズ
set -g history-limit 50000

### ステータスラインのキーバインドをemacs風にする
set -g status-keys emacs

# WARNING: Do not edit this file.
# It was generated by processing {{ yadm.source }}
